{
  "name": "olos-examples/api",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "vite build",
    "check-types": "wrangler types && tsc --noEmit",
    "deploy": "vite build && wrangler deploy",
    "dev": "vite dev",
    "gen-types": "wrangler types",
    "minio:down": "docker compose down -v",
    "minio:up": "docker compose up -d",
    "publish-demo": "bun scripts/publish-demo.ts",
    "reset": "bun scripts/reset.ts"
  },
  "dependencies": {
    "@arsenstorm/olos": "workspace:*",
    "@aws-sdk/client-s3": "^3.1066.0",
    "@aws-sdk/s3-request-presigner": "^3.1066.0"
  },
  "devDependencies": {
    "@cloudflare/vite-plugin": "^1.52.1",
    "@smithy/fetch-http-handler": "^5.5.2",
    "typescript": "^7",
    "vite": "^8",
    "wrangler": "^4"
  },
  "engines": {
    "node": ">=22.18.0"
  }
}
